package com.amazon.proserve.domain.flight

import com.amazon.proserve.domain.flight.FlightSpecial

import spock.lang.Specification

class FlightSpecialSpec extends Specification {

    // def "creation"() {
    //     when:
    //     FlightSpecial flightSpecial = FlightSpecial.builder()
    //             .header(FlightSpecial.of("HEADER-1"))
    //             .body(FlightSpecial.of("BODY-1"))
    //             .origin(FlightSpecial.of("ORIGIN-1"))
    //             .build()

    //     then:
    //     FlightSpecial != null && flightSpecial instanceof FlightSpecial
    // }
}
